{
  "name": "@latent-market/openclaw-plugin",
  "version": "1.0.0",
  "description": "Earn USDC from sponsored ads injected into your agent's thinking state. Open ad marketplace for AI agents on Base.",
  "license": "Apache-2.0",
  "homepage": "https://github.com/enzoonchain/latent-market",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "skills"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.1.9"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"]
  }
}
