{
  "id": "latent-protocol",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "wallet": {
        "type": "string",
        "description": "Base (EVM) wallet address that receives USDC earnings.",
        "pattern": "^0x[a-fA-F0-9]{40}$"
      },
      "enabled": {
        "type": "boolean",
        "description": "Master on/off switch for ad serving.",
        "default": true
      },
      "frequency": {
        "type": "number",
        "description": "Show the sponsored footer once every N final replies, per conversation.",
        "default": 1,
        "minimum": 1
      },
      "server": {
        "type": "string",
        "description": "Latent Protocol ad server base URL.",
        "default": "https://api.latentprotocol.xyz"
      },
      "minPayout": {
        "type": "number",
        "description": "Deprecated and ignored \u2014 balance and payouts are on the Latent dashboard. Kept so existing configs still validate.",
        "default": 5.0
      }
    },
    "required": [
      "wallet"
    ]
  }
}
